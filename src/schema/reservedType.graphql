extend type Query {
    reservedTypes: [ReservedType]
    reservedType(id: Int!): ReservedType
}

extend type Mutation {
    createReservedType(type_name: String!, description: String!): ReservedType
    updateReservedType(id: Int!, type_name: String!, description: String): ReservedType
    deleteReservedType(id: Int!): ReservedType
}

type ReservedType {
    id: Int!
    type_name: String!
    description: String
    createdAt: String!
    updatedAt: String!
}